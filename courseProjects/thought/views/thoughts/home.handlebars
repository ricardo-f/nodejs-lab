<div class="home-header">
  {{#if search}}
  <h1>You're searching for: <span>{{search}}</span></h1>
    {{#if thoughtQty}}
      <p>Found: <span>{{thoughtQty}}</span> thought matching your search</p>
    {{else}}
    <p>No results available for <span>{{search}}</span></p>
    {{/if}}
  {{else}}
  <h1>Listed thoughts</h1>
  {{/if}}
  <form action="/" method="GET">
    <input type="text" name="search" placeholder="Search for somenthing?">
    <input type="submit" class="btn" value="Search">
  </form>
  <div class="order-container">
    <span>Order by:</span>
    <form action="/" method="GET">
      <input type="hidden" name="search" value="{{this.search}}">
      <input type="hidden" name="order" value="new">
      <button type="submit"><i class="bi bi-arrow-up"></i></button>
    </form>
    <form action="/" method="GET">
      <input type="hidden" name="search" value="{{this.search}}">
      <input type="hidden" name="order" value="old">
      <button type="submit"><i class="bi bi-arrow-down"></i></button>
    </form>
    <a href="/">Clear</a>
  </div>
</div>
<div class="thoughts-container">
  {{#each thought}}
  <figure>
    <blockquote>
      " {{this.title}}"
    </blockquote>
    <figcaption>By: <span>{{this.User.name}}</span></figcaption>
  </figure>
  {{/each}}
</div>
