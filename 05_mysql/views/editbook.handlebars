<h1>Bookstore</h1>
<h2>Edit your book!</h2>
<form action="/books/updatebook" method="POST" class="form">
  <input type="hidden" name="id" value="{{book.id}}">
  <div class="form-control">
    <label for="title">Title:</label>
    <input type="text" name="title" placeholder="Enter your book title" value="{{book.title}}">
  </div>
  <div class="form-control">
    <label for="pageqty">Number of pages:</label>
    <input type="number" name="pageqty" placeholder="number of pages" value="{{book.pageqty}}">
  </div>
  <input type="submit" value="Edit">
</form>
